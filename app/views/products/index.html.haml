.gridRow
  /
    .col-md-2
      .row.row1.text-center
        %h3 Bộ lọc
        - Category.all.each do |category|
          = link_to "#{category.name}", category


  
  .product-list
    - @products.each do |product|
      .product-box
        .image
          = link_to image_tag(product.image.url),product
        .title
          = link_to product.title, product
        .des
          = product.description
        .price
          = number_to_currency(product.price, unit: "VNĐ ")
        .buttonclass
          = button_to  line_items_path(product_id: product), class: 'add-button', remote: true do
            %i.fa.fa-cart-plus
      
  .text-center.digg_pagination
    .pagination
      = will_paginate @productsheight